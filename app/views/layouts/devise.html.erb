<!DOCTYPE html>
<html lang="en-US">

<head>
  <meta charset="UTF-8">
  <title>MIND GT - Login</title>
   <%= stylesheet_link_tag 'LoginStyle', :media => 'screen' %>
  
 <%= csrf_meta_tags %>  
</head>

<body>
<div class= "login-card">
<%= image_tag 'http://mindhub.mx/wp-content/uploads/2014/11/mindWhite.png' %>
<div id="login">
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  
  <div class="field"><span class="fontawesome-user"></span>
    <%= f.email_field :email,  :value => 'email', :onBlur => "if(this.value == '') this.value = 'email'", :onFocus => "if(this.value == 'email') this.value = ''"  %>
  </div>

  <div class="field"> <span class="fontawesome-lock"></span>
    <%= f.password_field :password, autocomplete: "off", :value => 'password', :onBlur => "if(this.value == '') this.value = 'password'", :onFocus => "if(this.value == 'password') this.value = ''" %>
  </div>

  <div class="actions">
    <%= f.submit "Log in", autofocus: true %>
  </div>

  <center><p>All rights reserved.</p></center>

  <!-- Code to remember LogIn Info -->
  <% if devise_mapping.rememberable? -%>
    <div class="field">
    </div>
  <% end -%>

  <% end %>
</div>
</div>
<h2>Sign up</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true %>
  </div>

  <div class="field">
    <%= f.label :password %>
    <% if @validatable %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "off" %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off" %>
  </div>

  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
  </body>
</html>

